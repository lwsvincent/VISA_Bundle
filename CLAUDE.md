# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Git Push

This repository has both `origin` (GitHub) and `gitlab` remotes. When pushing commits, push to **both** remotes for the current branch (and tags, if applicable).

